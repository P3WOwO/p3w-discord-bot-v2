# P3W Discord Bot

## Что это
Discord-бот с:
- командой времени в войсе
- топом по войсу
- жизнью бота
- админскими командами `msg`, `purge`, `jtm`
- AI-ответами через Gemini
- генерацией изображений через Gemini Image с fallback-моделями
- хранением состояния в Supabase

## Что нужно создать
1. Проект в Supabase
2. Таблицу из `supabase_bot_state.sql`
3. Web Service в Render

## Переменные окружения
Обязательные:
- `TOKEN`
- `CLIENT_ID`
- `GUILD_ID`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

Для AI:
- `GEMINI_API_KEY`
- `GEMINI_MODEL` (необязательно, по умолчанию `gemini-2.5-flash-lite`)
- `GEMINI_CHAT_MODELS` (необязательно, список fallback-моделей через запятую)
- `GEMINI_IMAGE_MODELS` (необязательно, список fallback-моделей для картинок)
- `GEMINI_IMAGE_ASPECT_RATIO` (необязательно, по умолчанию `16:9`)
- `GEMINI_IMAGE_SIZE` (необязательно, по умолчанию `2K`)
- `AI_BASE_PROMPT` (необязательно, базовая настройка личности)

Дополнительно:
- `SUPABASE_TABLE` (необязательно, по умолчанию `bot_state`)
- `SUPABASE_ROW_ID` (необязательно, по умолчанию `main`)

## Установка локально
```bash
npm install
npm start
```

## Деплой на Render
- Создай Web Service из GitHub-репозитория
- Build Command: `npm install`
- Start Command: `npm start`
- Добавь переменные окружения в Render Dashboard
- Задеплой

## Supabase
- Открой SQL Editor
- Вставь `supabase_bot_state.sql`
- Нажми Run


## Память ИИ
Система памяти теперь хранит:
- краткую общую сводку,
- память по пользователю и по каналу,
- отдельный профиль бота с устойчивой позицией и стилем,
- авто-удаление дублей и слабых/устаревших заметок.

Бот сам решает, что сохранить, а что выкинуть, чтобы контекст не разрастался.


## Память ИИ v2
Теперь память работает в несколько слоёв:
- быстрый heuristic-pass ловит устойчивые факты даже без Gemini;
- Gemini пишет структурированный JSON для долговременной памяти;
- память разделяет предпочтения, ограничения, проекты и факты;
- противоположные факты вроде "люблю" и "не люблю" больше не сливаются;
- спорные записи уходят в pending review;
- retrieval стал сильнее за счёт более умного ранжирования.

## Новое поведение
- `!image <описание>` или `/image` — генерация картинки
- бот сам переключает fallback-модели при ошибках генерации
- статус в Discord меняется по реальному действию: ответ, генерация, память
- запросы про время в войсе могут отвечать напрямую по `voice_times`

